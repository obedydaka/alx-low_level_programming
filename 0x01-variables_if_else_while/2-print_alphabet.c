#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - begin
 * Description
 * Return: Always 0 (Success)
 */

int main(void)
{
	char llt;

	for (llt = 'a'; llt <= 'z'; llt++)
	{
		putchar (llt);
	}
	putchar ('\n');

	return (0);
}
