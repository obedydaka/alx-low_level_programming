#include "main.h"

/**
 * add - Check main
 * @a: integer a
 * @b: integer b
 * Description: This function adds two integers and returns the result
 * Return: The addition of a and b
 */

int add(int a, int b)
{
	int result;
	result = 0;

	result += (a + b);

	return (result);
}
